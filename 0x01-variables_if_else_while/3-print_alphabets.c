#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main- Enty point
 * Return: 0 (success)
*/
int main(void)
{
	char low;

	for (low = 'a'; low <= 'z'; low++)
		putchar(low);

	 for (low = 'A'; low <= 'Z'; low++)
		 putchar(low);
	         putchar('\n');
		 retuen (0);
}

